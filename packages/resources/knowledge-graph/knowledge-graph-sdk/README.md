# knowledge-graph-sdk

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build knowledge-graph-sdk` to build the library.

## Running unit tests

Run `nx test knowledge-graph-sdk` to execute the unit tests via [Jest](https://jestjs.io).
